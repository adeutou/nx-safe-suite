# @nx-safe-suite/env

## 0.1.0

### Minor Changes

- 83f9633: Initial implementation of `createEnv`: strict, type-safe validation of server, client, and shared environment variables backed by Zod, with configurable failure strategy (`exit` or `throw`) and client-prefix enforcement.
