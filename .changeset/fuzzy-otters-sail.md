---
"@nx-safe-suite/env": minor
---

Initial implementation of `createEnv`: strict, type-safe validation of server, client, and shared environment variables backed by Zod, with configurable failure strategy (`exit` or `throw`) and client-prefix enforcement.
